% ======================================================================
% proc's arg is proc. (value=29)
% (This is called higher-order function.)

(proc(f) (f 30)
 proc(x) -(x,1))

% ======================================================================
