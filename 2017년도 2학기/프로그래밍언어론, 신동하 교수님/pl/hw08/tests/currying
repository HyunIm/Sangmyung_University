% ======================================================================
% Currying. (value=12)

let f=proc(x) proc(y)
        begin
          set x=-(x,-1);
          -(x,y)
        end
in ((f 44) 33)

% ======================================================================
