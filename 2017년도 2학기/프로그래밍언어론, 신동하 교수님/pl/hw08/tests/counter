% ======================================================================
% function counter. (value=-1)

let g=let counter=0
      in proc(dummy) begin
                       set counter=-(counter,-1);
                       counter
                     end
in -((g 11),(g 22))

% ======================================================================
