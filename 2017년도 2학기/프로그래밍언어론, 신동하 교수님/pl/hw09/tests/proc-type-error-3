% ======================================================================
% proc. (value=type-error)

proc (x:int) proc(f:(int->(int->bool))) (f zero?(x))

% ======================================================================
